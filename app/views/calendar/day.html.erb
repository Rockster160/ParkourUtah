<% if @events %>
  <% @events.each do |event| %>
    <%= event.city %> - <%= event.host %> - <%= event.class_name.capitalize %> - <%= link_to "Event", event_path(event.id) %><br/><br/>
  <% end %>
<% else %>
  No events on this day.
<% end %>

<% date ||= @date %>
<% day ||= @day %>
<div class="a-single-date <%= date.to_date == day.to_date ? 'date' : '' %>" data-date="<%= day.to_date %>">
  <h5 class="text-center"><%= day.strftime('%A %B %d, %Y') %></h5>
  <br/>
  <% Event.by_date(day).each do |event| %>
    <div class="">
      <%= event.class_name %>
    </div>
  <% end %>
</div>

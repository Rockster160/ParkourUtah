<div class="fancy-box central-container form-offset">
  <h2>Events</h2>
  <br/>
  <% @events.each do |event| %>
    <h4><%= link_to edit_event_path(event.id), class: 'fancy-btn set-min-width-btn' do %>
      <%= "#{event.city} #{event.class_name}" %>
    <% end %></h4>
    <br/>
    <br/>
  <% end %>
</div>

<div class="modal fade" id="registration_required" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="central-container">
    <div class="error-container center-block">
      <button type="button" class="close-btn error-close" data-dismiss="modal" aria-hidden="true"><%= icon("times-circle-o", '', class: 'fa-2x') %></button>

      <h3 class="pkut-header">Registration Required</h3>
      <br/>
      <div class="row">
        <p>You must be registered and signed in to purchase accessories or class credits.</p>
        <p>If you already have an account, click the 'Sign In' button below.</p>
        <p>Otherwise, click on 'Register' in order to create a new account.</p>
        <a href="<%= new_user_session_path %>" class="pkut-btn">Sign In</a> <br> <br>
        <a href="<%= new_user_registration_path %>" class="pkut-btn">Register</a>
      </div>

    </div>
  </div>
</div>
